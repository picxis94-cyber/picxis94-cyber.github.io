// AUTO-GENERATED by tools/gen.mjs — do not edit.
window.BLOG_ARTICLES = [{"id":"flow-field-engine","title":"从零写一个风场粒子引擎","date":"2026-08-20","tags":["前端","Canvas","动画"],"excerpt":"台风眼是怎么形成的？这篇文章用一个 Canvas + Simplex Noise 的流场引擎，解释如何让数千颗粒子随\"风\"起舞，并让鼠标变成风暴之眼。","glyph":"🌀","cover":["#7c3aed","#f472b6"],"time":3,"words":953,"body":"你大概见过天气预报 App 里那种流动的风场：无数条纤细的流线掠过地图，仿佛空气真的在动。那种效果并不神秘——它只是一个 **流场（Flow Field）**，加上一堆追风的粒子。本文带你用最少的代码，从零实现一个极客版的风场，而且鼠标所到之处，会形成一个真正的\"风眼\"。\n\n## 一、流场是什么\n\n流场是一个函数：给定平面上任意一点 `(x, y)` 和时间 `t`，返回一个方向（角度）。粒子只是沿着这个方向前进。如果相邻区域的角度是**连续变化**的，画面就会呈现出平滑、流动的曲线；如果角度是随机的，就只是一团乱麻。\n\n问题来了：怎么让角度连续变化？答案是一张**连续噪声图**。最常见的工具是 Perlin Noise 与 Simplex Noise。这里我们用 Simplex Noise，它在高维下更快、且没有明显的方向性伪影。\n\n```js\n// 2D Simplex Noise 的关键：把输入点映射到三角形网格\n// 每个点由三个顶点做梯度插值，而非四角，\n// 因此维度越高，开销反而越低（O(N) 而非 O(2^N)）。\nconst n = noise.noise2D(x * 0.0016, y * 0.0016 + t * 0.12);\nreturn n * Math.PI * 4;   // 乘以 4π，让方向在 -2π ~ 2π 间缓慢扭转\n```\n\n注意那个 `t * 0.12`：把时间混进坐标，流场就会随时间\"呼吸\"，画面立刻活过来。\n\n## 二、粒子只是信使\n\n粒子本身不思考，它们只是把流场的角度变成位移：\n\n```js\nfunction update(p, t) {\n  const ang = fieldAngle(p.x, p.y, t);\n  p.x += Math.cos(ang) * p.speed;\n  p.y += Math.sin(ang) * p.speed;\n}\n```\n\n但直接更新 `(x, y)` 画一个点，看起来像灰尘。真正的\"丝线感\"来自**上一帧位置**：把上一帧和这一帧连成一条线段。速度越快，线段越长，视觉上就成了被风拉长的丝。这也是为什么天气预报的风场看起来像丝绸而不是烟花。\n\n## 三、制造风眼\n\n风眼（台风眼）在现实里是低压中心，空气围绕中心旋转。在代码里，我们给每个粒子叠加一个**旋涡力场**：以鼠标为中心，垂直方向加切向速度，让它绕圈；同时加一点点向心吸力，把粒子拉近。\n\n```js\nconst dx = p.x - mouse.x, dy = p.y - mouse.y;\nconst d  = Math.hypot(dx, dy) || 1;\nconst fall = influence(d);            // 距离越远影响越小\n\nconst tx = -dy / d, ty = dx / d;      // 切向（绕鼠标旋转）\nconst ix =  dx / d, iy = dy / d;      // 径向（指向鼠标）\n\np.x += (tx * swirl - ix * inward) * p.speed * fall;\np.y += (ty * swirl - iy * inward) * p.speed * fall;\n```\n\n最妙的一步是**核心掏空**：当粒子离鼠标非常近时，给它一个反向的径向力把它推开。这样旋涡中心反而空出一小块\"宁静区\"——真正的台风眼，永远是最平静的地方。\n\n## 四、性能要点\n\n1. **不要每帧新建对象**，用对象池复用粒子。\n2. **按设备像素比缩放画布**，但上限设为 2，防止 4K 屏被粒子数拖垮。\n3. **离屏后暂停**：`document.hidden` 时停掉 `requestAnimationFrame`，回来后继续。\n4. 移动端把粒子数砍到桌面端的 1/3，速度反而更顺。\n\n```js\nconst count = width * height < 500_000 ? 80 : 220;\n```\n\n## 五、还能玩出什么\n\n- 把鼠标换成**手指**，移动端也能捏出风眼。\n- 用同一套流场渲染**文字**——让粒子先组成\"404\"，鼠标划过就四散奔逃。这已经超出文章范围了，留作彩蛋。\n\n> 代码即风，你挥手的瞬间，就是风暴诞生的那一刻。\n\n完整的可运行版本就在这个博客的背景里——你现在看到的每一根流光，都是流场在呼吸。"},{"id":"go-concurrency-notes","title":"Go 并发三板斧：goroutine、channel、select","date":"2026-08-15","tags":["Go","后端","并发"],"excerpt":"不要通过共享内存来通信，而要通过通信来共享内存。用三个核心原语拆解 Go 的并发心智模型。","glyph":"🐹","cover":["#0d9488","#a78bfa"],"time":2,"words":575,"body":"Go 的并发模型就一句话：**不要通过共享内存来通信，而要通过通信来共享内存。** 翻译成人话——别让多个 goroutine 抢同一个变量，而是让它们互相\"传纸条\"。\n\n## goroutine：轻到没朋友\n\ngoroutine 不是线程，而是一个 **2KB 起步、按需增长**的协程，由 Go 运行时调度。创建一万个 goroutine 毫无压力，而一万个线程可能已经撑爆了。\n\n```go\ngo func() {\n    for {\n        select {\n        case <-ctx.Done():\n            return\n        default:\n            work()\n        }\n    }\n}()\n```\n\n关键洞察：goroutine 之间的\"切换\"由运行时在**协作点**完成（channel 收发、syscall、调度点），而非操作系统的抢占式时间片，所以它快、它省。\n\n## channel：就是那个纸条\n\nchannel 是一根管道。send 是塞纸条，receive 是取纸条。它默认是**同步**的：发送方会阻塞直到有人接收。\n\n```go\njobs := make(chan int, 8)   // 带缓冲，像收件箱\ndone := make(chan struct{}) // 无缓冲，纯信号\n\ngo func() {\n    for j := range jobs {    // 空 channel 关闭后自动退出\n        process(j)\n    }\n    close(done)\n}()\n```\n\n带缓冲的 channel 是\"异步邮箱\"，能减少阻塞；无缓冲的 channel 则是\"握手\"，天然适合做同步信号。**close 一个 channel 是广播**——所有接收方都会立刻拿到零值，这是优雅停止 worker 的标准姿势。\n\n## select：同时监听所有纸条\n\n`select` 会让 goroutine 同时等一堆 channel，谁先有消息就处理谁：\n\n```go\nselect {\ncase msg := <-inbox:\n    handle(msg)\ncase <-ticker.C:\n    heartbeat()\ncase <-ctx.Done():\n    return err\ndefault:\n    // 一个都没就绪：立刻返回，不阻塞\n}\n```\n\n`select` 加 `ctx` 是 Go 世界里最优雅的\"超时 + 取消\"组合拳。没有它，你很容易写出阻塞到死的 worker。\n\n## 常见翻车现场\n\n1. **只发不收**：没人消费 channel，goroutine 全堵死 → 用 `context` 或确保消费方存在。\n2. **重复 close**：`close` 一个已关闭的 channel 会 panic → 用 `sync.Once` 或只让一个 goroutine 负责关闭。\n3. **无锁共享变量**：多个 goroutine 同时写 map → 用 `sync.Mutex` 或直接改成 channel。\n\n```go\nvar mu sync.Mutex\ncount := make(map[string]int)\nmu.Lock()\ncount[key]++   // 天下太平\nmu.Unlock()\n```\n\n> 并发不是并行。并发是\"同时处理很多事\"，并行是\"同一时刻真的在做很多事\"。先学会并发，再谈并行。\n\n这套心智模型背下来，你的 Go 程序就很少再出现\"死锁现场\"了。"},{"id":"postgres-index-notes","title":"PostgreSQL 索引漫谈：从 B-Tree 到 BRIN","date":"2026-08-10","tags":["数据库","PostgreSQL","性能"],"excerpt":"什么时候该建索引？B-Tree、GIN、BRIN 各自适合什么场景？一篇帮你建立索引直觉的漫谈。","glyph":"🗄️","cover":["#7c3aed","#f472b6"],"time":2,"words":558,"body":"索引是数据库的\"作弊笔记\"。没有它，PostgreSQL 只能**顺序扫描**每一行；有了它，就像在书里插了目录，直接翻到对应的页码。但索引不是越多越好——它要占空间、拖慢写入。这篇聊聊几种主流索引的心智模型。\n\n## B-Tree：万金油\n\n默认索引类型。适合 `=`、`>`、`<`、`BETWEEN`、`ORDER BY`、前缀 `LIKE 'abc%'`。它的结构是**多层有序树**，叶子节点指向行。\n\n```sql\nCREATE INDEX idx_users_email ON users(email);\n```\n\n经验法则：\n\n- 选择性高（一个值对应行数很少）的列，索引收益最大。\n- 组合索引 `(a, b)` 能同时服务 `a`、`a+b` 的查询，但**服务不了**单独查 `b` 的查询——最左前缀原则。\n- `ORDER BY a DESC` 也能走 `(a)` 的索引，反序扫描几乎免费。\n\n## GIN：给数组和全文搜索\n\nGIN 是\"倒排索引\"，适合一个值对应很多行的场景：数组包含、全文搜索、JSONB 的 `@>` 查询。\n\n```sql\nCREATE INDEX idx_tags ON posts USING GIN (tags);\nCREATE INDEX idx_fts ON posts USING GIN (to_tsvector('english', body));\n```\n\n查询\"包含标签\"或\"包含某词\"时，B-Tree 无能为力，GIN 一下命中。\n\n## BRIN：为海量时序数据而生\n\nBRIN 把表的**物理块区间**记录最小值与最大值。如果数据是按时间顺序写入的（日志、传感器），每个区间内的最大值 / 最小值范围就很小，查询能瞬间跳过绝大多数块。\n\n```sql\nCREATE INDEX idx_ts ON events USING BRIN (created_at);\n```\n\nBRIN 极省空间（几 KB 对比 B-Tree 的几 MB），但只适合**物理有序、且查询按序**的数据。乱序插入的列用 BRIN 是灾难。\n\n## 如何验证索引有没有生效\n\n永远用 `EXPLAIN ANALYZE` 说话，而不是猜：\n\n```sql\nEXPLAIN ANALYZE SELECT * FROM users WHERE email = 'a@b.c';\n-- 看到 Seq Scan 说明没走索引，走 Index Scan / Bitmap Heap Scan 才对\n```\n\n一个小技巧：`EXPLAIN (ANALYZE, BUFFERS)` 会告诉你真正读了多少个 8KB 的页，这是判断成本最诚实的指标。\n\n> 索引是药，不是保健品。先有查询病，再开索引方。\n\n建索引之前，先问自己：这个查询真的慢吗？数据量大吗？如果表只有几千行，顺序扫描比索引还快。"},{"id":"dark-interface-design","title":"深色界面的设计原则：暗，但别瞎","date":"2026-08-06","tags":["设计","前端","体验"],"excerpt":"深色模式不是把白色背景翻成黑底就完事了。层次、色彩、阴影、对比度，处处是反直觉的细节。","glyph":"🌙","cover":["#5eead4","#0284c7"],"time":2,"words":669,"body":"\"暗黑风格\"做不好，很容易变成\"脏黑风格\"：一片死黑，看不清边界，层次全无。深色设计有一套**反直觉**的规则，这篇把这些规则讲清楚。\n\n## 一、纯黑是陷阱\n\n`#000000` 是深色设计的头号陷阱。纯黑周围的一切都会被衬得更亮，而 OLED 屏幕上的纯黑与真正的\"设计黑\"之间，观感差异极大。正确的做法是给背景一个**极低的亮度**，并带一点色相：\n\n```css\n:root {\n  --bg: #05080c;        /* 不是 #000，带一点点冷 */\n  --bg-2: #080d14;      /* 高于背景的层级 */\n  --panel: rgba(13, 20, 30, .55);\n  --line: rgba(120, 200, 220, .14);  /* 隔断线也要有颜色 */\n}\n```\n\n层级 = 亮度递进：背景最暗 → 面板稍亮 → 边框更亮。用户靠**亮度差**判断层级，而不是靠边框。\n\n## 二、文字对比度：别让眼睛受苦\n\n深色底上，纯白文字对比度过高，长时间阅读会刺眼。正文用 `#dbe7ef` 这类带冷色的浅灰，次要文字用更暗的灰，形成三级文字系统：\n\n```css\n--text: #dbe7ef;        /* 主内容 */\n--text-dim: #8296a6;    /* 次要 */\n--text-faint: #4d5f6e;  /* 弱化 / 装饰 */\n```\n\n> 规则：对比度至少 4.5:1（WCAG AA）。但\"能达标\"和\"好看\"之间，还有一个词叫克制。\n\n## 三、阴影在深色里是加法，不是减法\n\n浅色界面靠投影\"浮起来\"，深色界面投影几乎看不见，反而要用**内发光和边缘高光**。一个技巧：给卡片顶部加一条 1px 的微弱亮线，模拟光从上方落下：\n\n```css\n.panel {\n  box-shadow: inset 0 1px 0 rgba(255, 255, 255, .04);\n}\n```\n\n一条 4% 白的内高光，胜过十层黑投影。\n\n## 四、高亮色：降低饱和度\n\n同一种品牌色，浅色模式下很鲜艳，到了深色底上就会刺眼、发荧光。深色界面里的主色应该**降低饱和度、提高明度**，让它在暗底上\"呼吸\"而不\"尖叫\"：\n\n```css\n:root { --accent: #5eead4; }        /* 深色模式 */\n[data-theme=\"light\"] { --accent: #0d9488; }  /* 浅色模式再加深 */\n```\n\n同样的色相，两种模式两套值——这也是为什么好的设计系统会把颜色放进 CSS 变量，而不是写死。\n\n## 五、别忘了动效和玻璃\n\n深色界面天然适合 `backdrop-filter: blur()` 的毛玻璃，但**玻璃后面的内容会透出来**，糊成一片。所以玻璃只该出现在导航栏、浮层这些短暂停留的地方，正文区域保持不透。\n\n最后一条心法：\n\n> 暗黑不是主题，是舞台。灯光（高亮色）越少，舞台越高级。\n\n把\"暗黑\"当成一种氛围而非目标，你的界面自然会高级起来——就像这个博客一样。"},{"id":"static-site-mental-model","title":"极简博客的心智模型：没有后端的博客也是博客","date":"2026-07-30","tags":["架构","前端","随笔"],"excerpt":"一个不需要数据库、不需要服务器、双击 index.html 就能跑的博客，是怎么组织数据流的？","glyph":"🏔️","cover":["#7c3aed","#f472b6"],"time":2,"words":625,"body":"写博客最常见的第一反应是：上框架、上数据库、上后台。但多数个人博客的内容量，根本喂不饱一台服务器。这篇聊聊**零后端**博客的心智模型——它怎么组织数据、怎么路由、怎么升级。\n\n## 内容即数据\n\n把\"内容\"和\"程序\"分离：文章是纯 Markdown 文件，程序只负责把它们渲染出来。目录结构一目了然：\n\n```\nblog/\n├── index.html          # 唯一入口\n├── css/style.css\n├── js/main.js\n├── articles/           # 内容即数据\n│   ├── flow-field-engine.md\n│   └── go-concurrency-notes.md\n└── rss.xml             # 由脚本生成\n```\n\n关键决策：**文章是内容，不是数据库记录**。Markdown 文件 + 版本管理（git），比任何后台编辑器都可靠、可追溯。\n\n## 数据流：编译期 vs 运行时\n\n零后端博客有两条路：\n\n1. **编译期构建**：写一个脚本把 Markdown 打包进一个 `articles.js`。浏览器直接读内存里的数据，双击文件也能跑（`file://` 协议下 `fetch` 会被浏览器禁止）。\n2. **运行时拉取**：用 `fetch('articles/x.md')` 按需加载，文章多时更省流量，但必须通过 HTTP 服务器访问。\n\n```js\n// 优雅降级：先试 fetch，失败就退回内嵌数据\nasync function load(id) {\n  try {\n    const res = await fetch(`articles/${id}.md`);\n    if (res.ok) return parseFrontmatter(await res.text());\n  } catch (_) { /* file:// 下必然失败，忽略 */ }\n  return EMBEDDED[id];   // 打包进来的兜底\n}\n```\n\n两条路都留，一条在云端，一条在本地——本地草稿永远能看。\n\n## 路由：一个页面，N 种视图\n\n没有后端就没有 URL 路由，于是我们用\"视图切换\"模拟页面：主文档只有一个 `index.html`，`data-view` 属性决定显示哪一块。\n\n```js\nconst views = document.querySelectorAll('[data-view]');\nfunction go(view) {\n  views.forEach(v => v.classList.toggle('is-active', v.dataset.view === view));\n  window.scrollTo({ top: 0, behavior: 'smooth' });\n}\n```\n\n配合 `history.pushState`，还能把视图状态写进 URL，分享链接、刷新还原都不成问题。这是单页应用最朴素的形态，代价是框架约等于零。\n\n## 什么时候需要升级\n\n如果有一天你发现：\n\n- 文章超过几百篇，首屏要加载的数据太大；\n- 想要多人协作、评论、数据统计；\n- 想要一套完整的后台编辑器；\n\n那就乖乖上静态站点生成器（Astro / Vite）或后端。但在此之前——**先让内容流动起来**。\n\n> 博客的核心不是技术栈，是写作本身。零后端不是简陋，是克制。\n\n这篇文章本身，就是这个心智模型的证明。"},{"id":"poetry-of-code","title":"代码即诗歌：写给十年后自己的三句话","date":"2026-07-18","tags":["随笔","生活","编程"],"excerpt":"在无数个深夜修 bug 之后，我越来越觉得：好的代码和好的诗，底层是同一件事。","glyph":"📜","cover":["#0ea5e9","#a78bfa"],"time":2,"words":591,"body":"写代码第十年，我慢慢明白了一件事：**好的代码和好的诗，是同一件事。**\n\n诗把千言万语压进几行；好的函数把一个复杂概念压缩成一个清晰的签名。诗讲究节奏和留白；好的代码讲究命名和空行。诗不写废话；好的代码里没有一句多余的注释。\n\n```python\n# 诗：\n#   def life(**choices):\n#       for tomorrow in days:\n#           if love in choices:\n#               return we(together)\n```\n\n## 命名是诗人最锋利的刀\n\n> 命名是编程里最难的三件事之二，另外两件是缓存失效和命名。\n\n给一个变量取名，本质是\"给概念下定义\"。`data`、`temp`、`x1` 是偷懒的韵脚；`revenue`、`pendingQueue`、`lastPingAt` 才是精心打磨的句子。写不出好名字，往往不是词汇量的问题，是你根本没想清楚这个概念是什么。\n\n## 留白：注释该删就删\n\n最好的注释是**不需要的注释**——代码本身已经说清楚了。真正值得留的注释，只有三类：\n\n1. \"为什么\"（不是\"是什么\"）：`// 必须晚于 DNS 刷新，否则证书校验失败`\n2. 复杂算法的出处：`// 见 Knuth Vol.3 §5.2.4`\n3. 业务上的\"陷阱警示\"：`// 此处勿动，动了会全公司一起崩`\n\n其余的，都是噪音。删除它们，代码的呼吸就通畅了。\n\n## 重构：修改诗句，不改诗意\n\n诗歌和代码都不怕改，怕的是**改的时候不知道在改什么**。测试就是代码的\"定稿存档\"——改了韵脚，意思还在：\n\n```js\n// 重构前：三行 if 塞在业务代码里\nfunction ship(order) {\n  if (!order.paid) throw new Error('未支付');\n  if (order.items.length === 0) throw new Error('空订单');\n  // ... 50 行发货逻辑\n}\n\n// 重构后：校验是诗的开篇，业务是诗的正文\nfunction ship(order) {\n  assertPayable(order);\n  // ... 50 行发货逻辑\n}\n```\n\n## 写给十年后\n\n所以，写给十年后仍在写代码的自己：\n\n> 别急着追求\"炫酷\"的架构，先写好每一行会说话的诗。函数短一点，名字准一点，逻辑诚一点。\n>\n> 十年后你回头看，那些让你骄傲的不是哪个框架，而是你把\"用户下单\"这件事，写得像一首流畅的绝句。\n\n写代码的人很多，会写诗的代码很少。愿你的代码，读起来像诗，跑起来像闪电。"}];
